<div class="bs-example">
    <ul id="navHeader" class="nav nav-pills">
        <li>
          <div id="logo">OneList.</div>
        </li>
        <li><%= link_to "HOME", lists_path %></li>
        <li class="dropdown">
            <a href="#" data-toggle="dropdown" class="dropdown-toggle">ACTIONS <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><%= link_to "ADD A LIST", new_list_path %></li>
                <li><%= link_to "ADD A GROUP", new_group_path %></li>
                <li></li>
                <li></li>
                <li></li>
            </ul>
          <li class="dropdown">
              <a href="#" data-toggle="dropdown" class="dropdown-toggle">LISTS <b class="caret"></b></a>
              <ul class="dropdown-menu">
              </ul>
          <li class="dropdown">
              <a href="#" data-toggle="dropdown" class="dropdown-toggle">GROUPS <b class="caret"></b></a>
              <ul class="dropdown-menu">
              </ul>
          <li><%= link_to "INVITES", invites_path %></li>
          <li class="pull-right"><form method="POST" action="/session">
            <input name="authenticity_token" value="<%= form_authenticity_token() %>" type="hidden">
            <input name="_method" value="DELETE" type="hidden">
            <button>Logout</button>
          </form></li>
        <li class="dropdown pull-right">
            <a href="#" data-toggle="dropdown" class="dropdown-toggle">PROFILE <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><a href="#">Another action</a></li>
                <li class="divider"></li>
                <li><a href="#">Settings</a></li>
            </ul>
        </li>
    </ul>
    <hr id="headerDivider">

<!-- <nav class="navbar navbar-inverse">
  <div class="container-fluid">
Brand and toggle get grouped for better mobile display
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">OneList.</a>
    </div>
Collect the nav links, forms, and other content for toggling
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "ADD A LIST", new_list_path %></li>
        <li><%= link_to "ADD A GROUP", new_group_path %></li>
        <li><%= link_to "INVITES", invites_path %></li>
        <li><%= link_to "HOME", lists_path %></li>
      </ul>
        <form id="logout" class="navbar-form navbar-right" method="POST" action="/session">
          <input name="authenticity_token" value="<%= form_authenticity_token() %>" type="hidden">
          <input name="_method" value="DELETE" type="hidden">
          <button class="btn btn-default">Logout</button>
        </form>
        <div id="username" class="navbar-form navbar-right">Welcome!  <%= session[:username] %></div>
    </div>
  </div>
</nav> -->
<% if @group.single_list %>
<div id="main-container" class="container-fluid">
      <div class="row">
<!-- group info-->
        <div id="main-content" class="col-md-12">
          <div class="row">
          <div class="col-md-3">
            <h2><%= @group.name %>:</h2>
            <h4><%= @group.subtitle %></h4>
          </div>
<!-- invite link -->
          <% if @group.creator_id == session[:user_id]%>
            <div id="addButton" class="btn btn-info col-md-1"><%= link_to "Invite", new_group_invite_path(@group.id) %></div>
          <% end %>
<!-- GROUP MEMBER LIST-->
          <!-- <h4>Group Members: </h4>
          <% @members.each do |member| %>
              <div class="row">
                <div class="col-md-7"><%= member.name %></div>
              </div>
            <% end %> -->
<!-- LIST OF INVITES THAT ARE OUT-->
          <!-- <% if @group.creator_id == session[:user_id]%>
          <h4>Invites out: </h4>
          <% @invites.each do |invite| %>
              <div id="inline">
                <div id="name"><%= invite.name %></div>
                <div id="addButton" class="btn btn-danger"><%= link_to "delete", group_invite_path(@group,invite.id), :method=> "delete" %></div>
              </div>
                </br>


            <% end %>
          <% end %> -->

<!-- THE LIST FOR SINGLE LIST GROUPS-->
          <div id="addButton" class="btn btn-large btn-primary col-md-2"><%= link_to "ADD ITEM", new_list_item_path(@list.id) %></div>
          <div id="addButton" class="btn btn-success col-md-2"><%= link_to "SORT BY PRICE", group_path(@group.id, :sort => "price") %></div>
          <div id="addButton" class="btn btn-success col-md-2"><%= link_to "SORT BY QUANTITY", group_path(@group.id, :sort => "quantity") %></div>
          <% if @group.creator_id == session[:user_id]%>
            <div id="addButton" class="btn btn-danger col-md-2"><%= link_to "DELETE GROUP", group_path, :method=> "delete" %></div>
          <% end %>
        </div>
<div class="row">
          <!-- <h4>The List: </h4> -->
          <% if @group.creator_id == session[:user_id]%>
          <% @items.each do |item| %>
              <div id="items" class="col-md-3">
                <div><img id="imgIndex" src="<%= item.image_url %>"></div>
                </br>
                <div><a href="<%= item.url %>"><%= item.name %></a></div>
                <div>Price: <%= number_to_currency(item.price.to_i) %></div>
                <div>quantity: <%= item.quantity %></div>
                <div><%= link_to "delete", [item.list, item], :confirm => "Are you sure you want to delete this comment?", :method => :delete %> <%= link_to "edit", edit_list_item_path(@list, item, :type=> "edit") %></div>
              </div>
            <% end %>
          <% end %>
          <% if @group.creator_id != session[:user_id]%>

          <% @items.each do |item| %>
              <div id="items" class="col-md-3">
                <div><img id="imgIndex" src="<%= item.image_url %>"></div>
                </br>
                <div><a href="<%= item.url %>"><%= item.name %></a></div>
                <div>Price: <%= number_to_currency(item.price.to_i) %></div>
                <div>Quantity: <%= item.quantity %></div>
                <% if item.quantity == 1 && item.quantity_bought == nil %>
                  <% if item.bought == false %>
                  <div><%= link_to "buy", list_item_path(@list.id, item.id, :item => {:type=> ""}), :method => :put %></div>
                  <% elsif item.bought == true %>
                  <% buyer = User.find_by(id: item.bought_id) %>
                  <div>Bought by: <%= buyer.first_name %> <%= buyer.last_name %>
                    <% if session[:user_id] == item.bought_id %>
                    <%= link_to "cancel", list_item_path(@list.id, item,:item => {:type=> ""}), :method => :put %>
                    <% end %>
                  </div>
                  <% end %>
                <% else %>
                  <% if item.quantity == 0 %>
                    <div>
                      <%= "All Items Bought" %>
                    </div>
                  <% else %>
                    <div>
                      <%= link_to "Buy Some or All", edit_item_path(item, :type=> "quantity") %>
                    </div>
                  <% end %>
                <% end %>
              </div>
           <% end %>
          <% end %>


<!-- LINK TO LEAVE SINGLE LIST GROUP-->
          <% if @group.creator_id != session[:user_id] && @group.single_list %>
            <% memberId = Member.find_by(name: session[:username], group_id: @group.id) %>
            <%= link_to "LEAVE GROUP", group_member_path(@group, memberId), :method=> "delete" %>
            <% end %>
            <!-- LINK TO LEAVE A MULTI LIST GROUP -->
          <% if @group.creator_id != session[:user_id] && @group.multi_list %>
            <% multiListId = MultiList.find_by(user_id: session[:user_id]) %>
            <%= link_to "LEAVE GROUP", group_member_path(@group, multiListId), :method=> "delete" %>
            <% end %>
          </div>

<!-- SIDE BAR FEATURES -->
        <!-- <div id="sidebar" class="col-md-2">
          <div class="well well-large">
            <h4>Lists: </h4>
            <ul class="nav nav-pills"> -->

<!-- USER LISTS -->
            <!-- <% @lists.each do |list| %>
                <li><%= link_to list.name, user_list_path(list.user_id, list.id)%></li>
              <% end %>
            </ul>
          </div>
          <div class="well well-large"> -->
<!-- USER GROUPS -->
            <!-- <h4>Groups: </h4>
            <h6>Admin of:</h6>
            <ul class="nav nav-pills">
            <% @groups.each do |group| %>

                <li><%= link_to group.name, group_path(group.id) %></li>
              <% end %>
            </ul>
            <h6>Group member of:</h6>
            <ul class="nav nav-pills">
            <% @members.each do |member| %>
              <% group = Group.find_by(id: member.group_id) %>
                <li><%= link_to group.name, group_path(group.id) %></li>

              <% end %>

            </ul>

          </div> -->
          <!-- LINK TO DELETE GROUP -->
                    <!-- <% if @group.creator_id == session[:user_id]%>
                      <div id="addButton" class="btn btn-danger"><%= link_to "DELETE GROUP", group_path, :method=> "delete" %></div>
                    <% end %> -->

        <!-- </div>
        </div>
        </div> -->
<!-- SHOWS THE CURRENT GROUP MEMBERS LIST -->
<% elsif @group.multi_list %>
<div id="main-container" class="container-fluid">
  <div class="row">
    <div id="main-content" class="col-md-10">
      <h2><%= @group.name %>:</h2>
      <h4><%= @group.subtitle %></h4>
      <div id="addButton" class="btn btn-large btn-primary"><%= link_to "ADD ITEM", new_list_item_path(@list.id) %></div>
      <h4>Your List: </h4>

      <% @items.each do |item| %>
          <div id="itemIndex" class="col-md-2">
            <div><img id="imgIndex" src="<%= item.image_url %>"></div>
            </br>
            <div><a href="<%= item.url %>"><%= item.name %></a></div>
            <div>Price: <%= number_to_currency(item.price.to_i) %></div>
            <div><%= link_to "delete", [item.list, item], :confirm => "Are you sure you want to delete this comment?", :method => :delete %></div>
          </div>
        <% end %>





<!-- LINK TO LEAVE A MULTI LIST GROUP -->
      <% if @group.creator_id != session[:user_id] && @group.multi_list %>
        <% multiListId = MultiList.find_by(user_id: session[:user_id]) %>
        <%= link_to "LEAVE GROUP", group_member_path(@group, multiListId), :method=> "delete" %>
      <% end %>

    </div>
    <div id="sidebar" class="col-md-2">
      <div class="well well-large">
        <!-- invite link -->
        <% if @group.creator_id == session[:user_id]%>
          <div><%= link_to "Invite", new_group_invite_path(@group.id) %></div>
        <% end %>
        <div class="row">
          <div class="col-md-offset-1"><%= link_to "Group Home", group_path(@group.id)  %></div>
        </div>
        <h6>Group Members: </h6>
        <% @multiLists.each do |multiList| %>
          <% if multiList.user_id != session[:user_id] %>
            <% user = User.find_by(id: multiList.user_id) %>
            <% group = Group.find_by(id: multiList.group_id) %>
            <div class="row">
              <div class="col-md-offset-1"><%= link_to user.username, group_multi_list_path(group.id,multiList.id) %></div>
            </div>
          <% end %>
        <% end %>
        </div>
        <!-- LINK TO DELETE GROUP -->
              <% if @group.creator_id == session[:user_id]%>
                <div id="addButton" class="btn btn-danger"><%= link_to "DELETE GROUP", group_path, :method=> "delete" %></div>
              <% end %>

    </div>
  </div>
</div>
<% end %>
